[build]
builder = "nixpacks"
buildCommand = "cd server && npm install && npm run db:deploy && npm run build"

[deploy]
startCommand = "cd server && npm start"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
